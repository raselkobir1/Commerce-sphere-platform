# Development secrets — safe to commit (these are throwaway dev values, not production).
# Docker Compose reads this via env_file in docker-compose.override.yml.
# Copy to .env.production and fill in real values for production deployment.

# ── PostgreSQL ────────────────────────────────────────────────────────────────
POSTGRES_USER=commerce
POSTGRES_PASSWORD=commerce_pass

# ── JWT ───────────────────────────────────────────────────────────────────────
JWT_SECRET=CommerceSphere_SuperSecret_JWT_Key_2024_Min32Chars!!
JWT_ISSUER=CommerceSphere
JWT_AUDIENCE=CommerceSphereClients
JWT_EXPIRY_MINUTES=60

# ── Keycloak ──────────────────────────────────────────────────────────────────
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=admin_pass
KEYCLOAK_CLIENT_SECRET=CommerceSphereDevSecret2024!
