version: "3.9"

networks:
  commerce-net:
    driver: bridge

volumes:
  pgdata-auth:
  pgdata-product:
  pgdata-inventory:
  pgdata-cart:
  pgdata-notification:
  pgdata-chat:
  redis-data:
  kafka-data:

services:

  # ─── Infrastructure ───────────────────────────────────────────

  postgres-auth:
    image: postgres:16-alpine
    container_name: postgres-auth
    restart: unless-stopped
    environment:
      POSTGRES_DB: auth_db
      POSTGRES_USER: commerce
      POSTGRES_PASSWORD: commerce_pass
    volumes:
      - pgdata-auth:/var/lib/postgresql/data
    networks:
      - commerce-net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U commerce -d auth_db"]
      interval: 10s
      timeout: 5s
      retries: 5

  postgres-product:
    image: postgres:16-alpine
    container_name: postgres-product
    restart: unless-stopped
    environment:
      POSTGRES_DB: product_db
      POSTGRES_USER: commerce
      POSTGRES_PASSWORD: commerce_pass
    volumes:
      - pgdata-product:/var/lib/postgresql/data
    networks:
      - commerce-net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U commerce -d product_db"]
      interval: 10s
      timeout: 5s
      retries: 5

  postgres-inventory:
    image: postgres:16-alpine
    container_name: postgres-inventory
    restart: unless-stopped
    environment:
      POSTGRES_DB: inventory_db
      POSTGRES_USER: commerce
      POSTGRES_PASSWORD: commerce_pass
    volumes:
      - pgdata-inventory:/var/lib/postgresql/data
    networks:
      - commerce-net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U commerce -d inventory_db"]
      interval: 10s
      timeout: 5s
      retries: 5

  postgres-cart:
    image: postgres:16-alpine
    container_name: postgres-cart
    restart: unless-stopped
    environment:
      POSTGRES_DB: cart_db
      POSTGRES_USER: commerce
      POSTGRES_PASSWORD: commerce_pass
    volumes:
      - pgdata-cart:/var/lib/postgresql/data
    networks:
      - commerce-net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U commerce -d cart_db"]
      interval: 10s
      timeout: 5s
      retries: 5

  postgres-notification:
    image: postgres:16-alpine
    container_name: postgres-notification
    restart: unless-stopped
    environment:
      POSTGRES_DB: notification_db
      POSTGRES_USER: commerce
      POSTGRES_PASSWORD: commerce_pass
    volumes:
      - pgdata-notification:/var/lib/postgresql/data
    networks:
      - commerce-net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U commerce -d notification_db"]
      interval: 10s
      timeout: 5s
      retries: 5

  postgres-chat:
    image: postgres:16-alpine
    container_name: postgres-chat
    restart: unless-stopped
    environment:
      POSTGRES_DB: chat_db
      POSTGRES_USER: commerce
      POSTGRES_PASSWORD: commerce_pass
    volumes:
      - pgdata-chat:/var/lib/postgresql/data
    networks:
      - commerce-net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U commerce -d chat_db"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: redis
    restart: unless-stopped
    command: ["redis-server", "--appendonly", "yes", "--maxmemory", "256mb", "--maxmemory-policy", "allkeys-lru"]
    volumes:
      - redis-data:/data
    networks:
      - commerce-net
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Kafka in KRaft mode (no ZooKeeper) — confluentinc/cp-kafka
  kafka:
    image: confluentinc/cp-kafka:latest
    container_name: kafka
    restart: unless-stopped
    environment:
      KAFKA_NODE_ID: 1
      KAFKA_PROCESS_ROLES: broker,controller
      KAFKA_CONTROLLER_QUORUM_VOTERS: 1@kafka:9093
      CLUSTER_ID: "MkU3OEVBNTcwNTJENDM2Qg"
      KAFKA_LISTENERS: PLAINTEXT://0.0.0.0:9092,CONTROLLER://0.0.0.0:9093,EXTERNAL://0.0.0.0:9094
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:9092,EXTERNAL://localhost:9094
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT,EXTERNAL:PLAINTEXT
      KAFKA_INTER_BROKER_LISTENER_NAME: PLAINTEXT
      KAFKA_CONTROLLER_LISTENER_NAMES: CONTROLLER
      KAFKA_AUTO_CREATE_TOPICS_ENABLE: "true"
      KAFKA_DEFAULT_REPLICATION_FACTOR: 1
      KAFKA_NUM_PARTITIONS: 3
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 1
      KAFKA_LOG_DIRS: /var/lib/kafka/data
    volumes:
      - kafka-data:/var/lib/kafka/data
    ports:
      - "9094:9094"
    networks:
      - commerce-net
    healthcheck:
      test: ["CMD-SHELL", "kafka-topics --bootstrap-server localhost:9092 --list || exit 1"]
      interval: 15s
      timeout: 10s
      retries: 10
      start_period: 30s

  # ─── Microservices ────────────────────────────────────────────

  auth-service:
    build:
      context: .
      dockerfile: src/Services/Auth/CommerceSphere.AuthService.API/Dockerfile
    container_name: auth-service
    restart: unless-stopped
    environment:
      ASPNETCORE_ENVIRONMENT: Production
      ASPNETCORE_URLS: http://+:80
      ConnectionStrings__AuthDb: "Host=postgres-auth;Port=5432;Database=auth_db;Username=commerce;Password=commerce_pass;"
      ConnectionStrings__Redis: "redis:6379,abortConnect=false,connectRetry=3"
      Jwt__Secret: "CommerceSphere_SuperSecret_JWT_Key_2024_Min32Chars!!"
      Jwt__Issuer: "CommerceSphere"
      Jwt__Audience: "CommerceSphereClients"
      Jwt__ExpiryMinutes: "60"
      Kafka__BootstrapServers: "kafka:9092"
      # Social login (OAuth) — provider credentials come from each provider's developer console.
      # Fill these in (e.g. via .env / secrets) to enable a provider; blank = provider hidden.
      Sso__CallbackBaseUrl: "http://localhost:5000"
      Sso__CallbackPath: "/api/auth/sso/callback"
      Sso__AllowedRedirectUris__0: "http://localhost:4200"
      Sso__AllowedRedirectUris__1: "http://localhost:4300"
      Sso__Providers__google__ClientId: "${GOOGLE_CLIENT_ID:-}"
      Sso__Providers__google__ClientSecret: "${GOOGLE_CLIENT_SECRET:-}"
      Sso__Providers__facebook__ClientId: "${FACEBOOK_CLIENT_ID:-}"
      Sso__Providers__facebook__ClientSecret: "${FACEBOOK_CLIENT_SECRET:-}"
    depends_on:
      postgres-auth:
        condition: service_healthy
      redis:
        condition: service_healthy
      kafka:
        condition: service_healthy
    networks:
      - commerce-net
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:80/health || exit 1"]
      interval: 15s
      timeout: 10s
      retries: 5
      start_period: 20s

  product-service:
    build:
      context: .
      dockerfile: src/Services/Product/CommerceSphere.ProductService.API/Dockerfile
    container_name: product-service
    restart: unless-stopped
    environment:
      ASPNETCORE_ENVIRONMENT: Production
      ASPNETCORE_URLS: http://+:80
      ConnectionStrings__ProductDb: "Host=postgres-product;Port=5432;Database=product_db;Username=commerce;Password=commerce_pass;"
      ConnectionStrings__Redis: "redis:6379,abortConnect=false,connectRetry=3"
      Jwt__Secret: "CommerceSphere_SuperSecret_JWT_Key_2024_Min32Chars!!"
      Jwt__Issuer: "CommerceSphere"
      Jwt__Audience: "CommerceSphereClients"
      Kafka__BootstrapServers: "kafka:9092"
    depends_on:
      postgres-product:
        condition: service_healthy
      redis:
        condition: service_healthy
      kafka:
        condition: service_healthy
    networks:
      - commerce-net
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:80/health || exit 1"]
      interval: 15s
      timeout: 10s
      retries: 5
      start_period: 20s

  inventory-service:
    build:
      context: .
      dockerfile: src/Services/Inventory/CommerceSphere.InventoryService.API/Dockerfile
    container_name: inventory-service
    restart: unless-stopped
    environment:
      ASPNETCORE_ENVIRONMENT: Production
      ASPNETCORE_URLS: http://+:80
      ConnectionStrings__InventoryDb: "Host=postgres-inventory;Port=5432;Database=inventory_db;Username=commerce;Password=commerce_pass;"
      ConnectionStrings__Redis: "redis:6379,abortConnect=false,connectRetry=3"
      Jwt__Secret: "CommerceSphere_SuperSecret_JWT_Key_2024_Min32Chars!!"
      Jwt__Issuer: "CommerceSphere"
      Jwt__Audience: "CommerceSphereClients"
      Kafka__BootstrapServers: "kafka:9092"
    depends_on:
      postgres-inventory:
        condition: service_healthy
      redis:
        condition: service_healthy
      kafka:
        condition: service_healthy
    networks:
      - commerce-net
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:80/health || exit 1"]
      interval: 15s
      timeout: 10s
      retries: 5
      start_period: 20s

  cart-service:
    build:
      context: .
      dockerfile: src/Services/Cart/CommerceSphere.CartService.API/Dockerfile
    container_name: cart-service
    restart: unless-stopped
    environment:
      ASPNETCORE_ENVIRONMENT: Production
      ASPNETCORE_URLS: http://+:80
      ConnectionStrings__CartDb: "Host=postgres-cart;Port=5432;Database=cart_db;Username=commerce;Password=commerce_pass;"
      ConnectionStrings__Redis: "redis:6379,abortConnect=false,connectRetry=3"
      Jwt__Secret: "CommerceSphere_SuperSecret_JWT_Key_2024_Min32Chars!!"
      Jwt__Issuer: "CommerceSphere"
      Jwt__Audience: "CommerceSphereClients"
      Kafka__BootstrapServers: "kafka:9092"
    depends_on:
      postgres-cart:
        condition: service_healthy
      redis:
        condition: service_healthy
      kafka:
        condition: service_healthy
      inventory-service:
        condition: service_healthy
    networks:
      - commerce-net
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:80/health || exit 1"]
      interval: 15s
      timeout: 10s
      retries: 5
      start_period: 20s

  notification-service:
    build:
      context: .
      dockerfile: src/Services/Notification/CommerceSphere.NotificationService.API/Dockerfile
    container_name: notification-service
    restart: unless-stopped
    environment:
      ASPNETCORE_ENVIRONMENT: Production
      ASPNETCORE_URLS: http://+:80
      ConnectionStrings__NotificationDb: "Host=postgres-notification;Port=5432;Database=notification_db;Username=commerce;Password=commerce_pass;"
      Jwt__Secret: "CommerceSphere_SuperSecret_JWT_Key_2024_Min32Chars!!"
      Jwt__Issuer: "CommerceSphere"
      Jwt__Audience: "CommerceSphereClients"
      Kafka__BootstrapServers: "kafka:9092"
      Email__SmtpHost: "mailpit"
      Email__SmtpPort: "1025"
    depends_on:
      postgres-notification:
        condition: service_healthy
      kafka:
        condition: service_healthy
    networks:
      - commerce-net
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:80/health || exit 1"]
      interval: 15s
      timeout: 10s
      retries: 5
      start_period: 20s

  chat-service:
    build:
      context: .
      dockerfile: src/Services/Chat/CommerceSphere.ChatService.API/Dockerfile
    container_name: chat-service
    restart: unless-stopped
    environment:
      ASPNETCORE_ENVIRONMENT: Production
      ASPNETCORE_URLS: http://+:80
      ConnectionStrings__ChatDb: "Host=postgres-chat;Port=5432;Database=chat_db;Username=commerce;Password=commerce_pass;"
      Jwt__Secret: "CommerceSphere_SuperSecret_JWT_Key_2024_Min32Chars!!"
      Jwt__Issuer: "CommerceSphere"
      Jwt__Audience: "CommerceSphereClients"
    depends_on:
      postgres-chat:
        condition: service_healthy
    networks:
      - commerce-net
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:80/health || exit 1"]
      interval: 15s
      timeout: 10s
      retries: 5
      start_period: 20s

  # ─── API Gateway ─────────────────────────────────────────────

  api-gateway:
    build:
      context: .
      dockerfile: src/ApiGateway/CommerceSphere.ApiGateway/Dockerfile
    container_name: api-gateway
    restart: unless-stopped
    ports:
      - "5000:80"
    environment:
      ASPNETCORE_ENVIRONMENT: Production
      ASPNETCORE_URLS: http://+:80
      Jwt__Secret: "CommerceSphere_SuperSecret_JWT_Key_2024_Min32Chars!!"
      Jwt__Issuer: "CommerceSphere"
      Jwt__Audience: "CommerceSphereClients"
    depends_on:
      auth-service:
        condition: service_healthy
      product-service:
        condition: service_healthy
      inventory-service:
        condition: service_healthy
      cart-service:
        condition: service_healthy
      notification-service:
        condition: service_healthy
      chat-service:
        condition: service_healthy
    networks:
      - commerce-net
